import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "rgb(0, 0, 0)",
    },
    secondary: {
      main: "rgb(255, 115, 0)",
      dark: "rgba(255, 115, 0, 0.922)",
    },
  },
});

export default theme;
